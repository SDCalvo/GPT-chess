export const fetchBlackMove = async () => {
  // This is a dummy function for now, later it will make a real API call
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve(null); // Resolve with a null move for now
    }, 1000); // Simulate network delay
  });
};
